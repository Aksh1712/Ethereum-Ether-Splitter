{
  "name": "ether-splitter",
  "version": "1.0.0",
  "description": "Ethereum smart contract for splitting Ether among three fixed addresses",
  "scripts": {
    "compile": "truffle compile",
    "migrate": "truffle migrate",
    "test": "truffle test",
    "verify": "truffle run verify EtherSplitter --network sepolia"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.0",
    "@openzeppelin/test-helpers": "^0.5.15",
    "@truffle/hdwallet-provider": "^2.1.0",
    "truffle": "^5.5.0",
    "truffle-plugin-verify": "^0.5.20",
    "web3": "^1.8.0",
    "dotenv": "^16.0.0"
  }
}